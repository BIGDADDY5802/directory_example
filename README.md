# directory_example

This is an example repo of pushing local directories from
your machine to github.


echo "# directory_example" >> README.md

git init

git add README.md

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/BIGDADDY5802/directory_example.git

git push -u origin main


git remote add origin https://github.com/BIGDADDY5802/directory_example.git

git branch -M main

git push -u origin main


Homework is submitted through github repository.

git add .

git commit -m "first commit"

git push -u origin main